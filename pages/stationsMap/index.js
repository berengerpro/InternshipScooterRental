import StationsMapPage from './StationsMapVM'
// import { StationRedux } from '@app/redux/reducers'

/* StationsMapPage.getInitialProps = async ({ req, ctx }) => {
  /* const { dispatch } = ctx.store
  ctx.store.dispatch(StationRedux.Creators.getStationsRequest())
  const userAgent = req ? req.headers['user-agent'] : 'test'
    return { userAgent }
} */

export default StationsMapPage
